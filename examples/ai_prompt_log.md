# AI Prompt Log

1. "Implement a minimal Python Agent Runtime that uses custom JSON actions, local tool dispatch, JSON session memory, and trace files. Do not use LangChain or provider-side function calling."

2. "Design tests first for calculator safety, file-backed session memory, and a runtime loop that handles a mock LLM tool_call followed by final_answer."

3. "Write documentation explaining the memory recall timing, prompt placement, trace schema, and a cross-turn task continuation demo."
